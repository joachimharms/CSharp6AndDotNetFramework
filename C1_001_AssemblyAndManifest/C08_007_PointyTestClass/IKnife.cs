namespace C08_007_PointyTestClass
{
    // Dieses Interface wurde automatisch durch die Aktion "Extract interface..." des Quick Action Menüs implementiert.

    // Visual studio also supports extract interface refactoring, available from the extract interface option of
    // the Quick actions menu.this allows you to pull out a new interface definition from an existing class definition.
    // for example, you might be halfway through writing a class when it dawns on you that you can generalize the
    // behavior into an interface (and thereby open up the possibility of alternative implementations).

    interface IKnife
    {
        int MyProperty { get; set; }
    }
}